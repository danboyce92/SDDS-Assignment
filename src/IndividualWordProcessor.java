import java.util.HashMap;

// public class IndividualWordProcessor {
//     /*Purpose of this class is to take a string, check if it exists in the GoogleMap.
//     If it does, change nothing.
//     If it does not, take the original words embedding and find the closest embedding to it inside the Google Array and replace it with that word.

//     */

//     WordSwapper ws = new WordSwapper();

//     public String findAlternateWord(String wordToChange, HashMap<String, double[]> googleWords, HashMap<String, double[]> allWords) {
//         if (googleWords.containsKey(wordToChange)) {
//             return wordToChange;
//         } else {
//             double[] wordToChangeEmbed = allWords.get(wordToChange);

//         }
//     }

// }
